# 🔷 Encapsulation (এনক্যাপসুলেশন)

### 📖 সংজ্ঞা:

**Encapsulation** হলো এমন একটি OOP ধারণা যেখানে ডেটা এবং মেথডগুলোকে একসাথে ক্লাসে রেখে বাইরের দুনিয়া থেকে লুকিয়ে রাখা হয়, যেন কেউ সরাসরি পরিবর্তন করতে না পারে।

---

### ✅ কেন দরকার?

* ডেটাকে বাইরের অবাঞ্ছিত অ্যাক্সেস থেকে সুরক্ষিত রাখতে।
* কোড মেইন্টেইনেবল, ফ্লেক্সিবল ও বাগ-প্রুফ করতে।

---

### 🧪 উদাহরণ: Bank Account

```tsx
class BankAccount {
  public readonly id: number;
  public name: string;
  private _balance: number;

  constructor(id: number, name: string, balance: number) {
    this.id = id;
    this.name = name;
    this._balance = balance;
  }

  deposit(amount: number): void {
    if (amount > 0) {
      this._balance += amount;
    }
  }

  withdraw(amount: number): void {
    if (amount > 0 && amount <= this._balance) {
      this._balance -= amount;
    }
  }

  getBalance(): number {
    return this._balance;
  }
}

const karimAccount = new BankAccount(101, "Mr. Karim", 5000);

karimAccount.deposit(2000);
karimAccount.withdraw(1000);
console.log(karimAccount.getBalance()); // 6000
```

🔍 এখানে:

* `_balance` প্রাইভেট হওয়ায় বাইরের কেউ সরাসরি পরিবর্তন করতে পারছে না।
* `deposit`, `withdraw`, এবং `getBalance` এর মাধ্যমে কন্ট্রোলড অ্যাক্সেস পাচ্ছে।
* এটি হলো **Encapsulation** — সবকিছু ক্লাসের ভিতরে রাখো, বাইরের অ্যাক্সেস সীমিত রাখো।

---

## ✅ উপকারিতা:

| সুবিধা       | ব্যাখ্যা                                        |
| ------------ | ----------------------------------------------- |
| নিরাপত্তা    | ডেটা সরাসরি পরিবর্তন হওয়া বন্ধ হয়               |
| কন্ট্রোল     | কিভাবে ডেটা পরিবর্তন হবে তা ক্লাস নিজেই ঠিক করে |
| রিইউজেবল কোড | সহজেই একই কোড অন্যত্র ব্যবহার করা যায়           |
| মেইন্টেইনেবল | বাগ খুঁজে বের করা সহজ হয়                        |

---

### ✅ সংক্ষেপে মনে রাখার নিয়ম:

> 🔒 **Encapsulation** = ডেটাকে প্রাইভেট করে কন্ট্রোলড অ্যাক্সেস দাও

> 🧰 মেথড ব্যবহার করেই বাইরের দুনিয়া ডেটা অ্যাক্সেস বা পরিবর্তন করতে পারবে

---
