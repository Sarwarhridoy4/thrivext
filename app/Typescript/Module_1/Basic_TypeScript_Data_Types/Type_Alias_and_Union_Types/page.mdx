## 🟩 8. Type Alias (টাইপ অ্যালিয়াস)

### 🔹 ১. অবজেক্ট টাইপ অ্যালিয়াস

```tsx
type UserName = {
  name: string;
  age: number;
};

const user: UserName = {
  name: "Alice",
  age: 25,
};
```

🧠 **ব্যাখ্যা:**

- `type` ব্যবহার করে `UserName` নামে একটি টাইপ তৈরি করা হয়েছে।
- এটি একটি অবজেক্ট টাইপ যেটিতে `name` (string) এবং `age` (number) থাকতে হবে।
- টাইপের নাম সবসময় Capital Letter দিয়ে শুরু করা ভালো অভ্যাস।

---

### 🔹 ২. প্রিমিটিভ টাইপের অ্যালিয়াস

```tsx
// উদাহরণ:
type MyString = string;
type MyNumber = number;
type MyBool = boolean;
type MyNull = null;
type MyUndefined = undefined;
```

🧠 **ব্যাখ্যা:**

- টাইপ অ্যালিয়াস কেবল অবজেক্ট বা ফাংশন টাইপের জন্য না — যেকোনো **প্রিমিটিভ টাইপ** (যেমন string, number ইত্যাদি)-এর জন্যও ব্যবহার করা যায়।

---

### 🔹 ৩. ফাংশন টাইপ অ্যালিয়াস

```tsx
type ADD = (a: number, b: number) => number;

const add: ADD = (a, b) => a + b;
```

🧠 **ব্যাখ্যা:**

- এখানে `ADD` নামে একটি ফাংশন টাইপ তৈরি করা হয়েছে।
- এটি দুইটি `number` ইনপুট নেয় এবং একটি `number` রিটার্ন করে।
- এরপর `add` ফাংশনটি `ADD` টাইপ অনুসারে ডিফাইন করা হয়েছে।

---

## 🔚 সংক্ষেপে:

| বিষয়                 | ব্যবহার                       | উদাহরণ                                        |
| -------------------- | ----------------------------- | --------------------------------------------- |
| টাইপ অ্যালিয়াস      | বারবার একই টাইপ ব্যবহার এড়াতে | `type User = { name: string; age: number }`   |
| প্রিমিটিভ অ্যালিয়াস | সহজ করে টাইপ নাম দেওয়া;       | `type Age = number;`                          |
| ফাংশন অ্যালিয়াস     | ফাংশনের টাইপ নির্ধারণ         | `type Add = (a: number, b: number) => number` |

## 🟩 9. Union Types (ইউনিয়ন টাইপস)

```tsx
type User = {
  name: string;
  age: number;
  gender: "male" | "female" | "other";
};

const user1: User = {
  name: "Rafsan",
  age: 22,
  gender: "male",
};
console.log(user1);
```

🧠 **ব্যাখ্যা:**

- `gender` ফিল্ডে শুধুমাত্র `"male"`, `"female"`, বা `"other"` টাইপের মান রাখা যাবে।
- এটি হলো **literal union type** — মানগুলোর একটি তালিকা।

📝 এটি টাইপ সুরক্ষার জন্য গুরুত্বপূর্ণ, যেন ব্যবহারকারী ভুল ইনপুট না দিতে পারে।
