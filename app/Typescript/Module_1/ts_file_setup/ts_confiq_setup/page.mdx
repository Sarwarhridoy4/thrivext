
# 🛠️ `tsconfig.json` setup

যখন তুমি একটু সিরিয়াসভাবে বা টিমে/প্রজেক্টে কাজ করবে — তখন নিচের মতো সেটআপ করাই ভালো: TypeScript প্রজেক্টে কীভাবে `.ts` ফাইল কম্পাইল হবে, তা কনফিগার করার জন্য `tsconfig.json` দরকার হয়। এটি তৈরি করতে:

```bash
tsc --init
```

এই কমান্ড চালালে প্রজেক্টের রুট ফোল্ডারে একটি `tsconfig.json` ফাইল তৈরি হবে।

---

## ⚙️ tsconfig.json কনফিগারেশন

### ১. **rootDir**:

এই অপশনটি জানায় যে তোমার প্রোজেক্টের সোর্স কোড কোথায় অবস্থিত। সাধারণত `.ts` ফাইলগুলো এখানে থাকবে।যদি তুমি `rootDir: "src"` সেট করো, তাহলে TypeScript শুধুমাত্র `src` ফোল্ডারের ফাইলগুলোকেই সোর্স হিসেবে মনে করবে। অর্থাৎ, তুমি যেসব `.ts` ফাইল লেখো সেগুলোর সবই `src` ফোল্ডারে রাখতে হবে।

```json
{
  "compilerOptions": {
    "rootDir": "src"  // সোর্স ফোল্ডার থাকবে 'src'
  }
}
```

---

### ২. **outDir**:

এই অপশনটি জানায় যে, কম্পাইল হওয়ার পর `.js` ফাইলগুলো কোথায় যাবে।
যদি তুমি `outDir: "dist"` সেট করো, তাহলে TypeScript `.ts` ফাইলগুলোর কম্পাইলড `.js` ভার্সনগুলো `dist` ফোল্ডারে রাখবে।

```json
{
  "compilerOptions": {
    "outDir": "dist"  // কম্পাইল হওয়া .js ফাইলগুলো যাবে 'dist' ফোল্ডারে
  }
}
```

---

### ৩. **Strict Mode (strict)**:

এই সেটিংটি TypeScript এর স্ট্রিক্ট টাইপ চেকিং চালু করে, যাতে ভুল টাইপ বা কোডের ভুল শনাক্ত করতে TypeScript বেশি সজাগ থাকে।

```json
{
  "compilerOptions": {
    "strict": true  // স্ট্রিক্ট টাইপ চেকিং চালু
  }
}
```

---

### ৪. **Include**:

এই সেটিংটি বলে দেয় TypeScript কে কোন ফোল্ডারগুলোতে `.ts` ফাইল অনুসন্ধান করতে হবে। এখানে `"include": ["src"]` অর্থাৎ TypeScript কেবল `src` ফোল্ডারটিই পরীক্ষা করবে।

```json
{
  "include": ["src"]  // .ts ফাইলগুলো শুধু 'src' ফোল্ডারে থাকবে
}
```

---

## **tsconfig.json উদাহরণ**

সবগুলো কনফিগারেশন একসাথে:

```json
{
  "compilerOptions": {
    "target": "ES6",           // কম্পাইল করা কোড হবে ES6 ফর্ম্যাটে
    "module": "commonjs",      // কম্পাইল করা কোডের মডিউল সিস্টেম হবে commonjs
    "rootDir": "src",          // সোর্স ফোল্ডার থাকবে src
    "outDir": "dist",          // কম্পাইল হওয়া .js ফাইলগুলো যাবে dist ফোল্ডারে
    "strict": true             // স্ট্রিক্ট টাইপ চেকিং চালু
  },
  "include": ["src"]           // .ts ফাইলগুলো শুধু src ফোল্ডারে থাকবে
}
```

### নতুন `target` পরিবর্তন:

TypeScript এর `target` অপশনটি কম্পাইলড কোডের JavaScript ভার্সন নির্ধারণ করে। যখন তুমি নতুন JavaScript ফিচার ব্যবহার করতে চাও, তখন `target` মান পরিবর্তন করা উচিত।

```json
{
  "compilerOptions": {
    "target": "ES6"           // কম্পাইল করা কোড হবে ES6 ফর্ম্যাটে
  }
}
```

**যেমন**: 
- **ES6**:
 ```json
"target": "ES6"           // কম্পাইল করা কোড হবে ES6 ফর্ম্যাটে
```
    
- **ESNext**:
```json
"target": "ESNext"        // সর্বশেষ JavaScript ফিচার ব্যবহারের জন্য
```
    

### **target অপশন** এর মান পরিবর্তন করতে হবে যখন:

- নতুন JavaScript ফিচার ব্যবহার করতে চাও।
- পুরনো ব্রাউজারে সমর্থিত কোড কম্পাইল করতে চাও।

---

## ✅ প্রোজেক্ট স্ট্রাকচার

TypeScript প্রজেক্টে ফোল্ডার স্ট্রাকচার এইভাবে হওয়া উচিত:

```
your-project/
│
├── src/                    <-- সোর্স ফাইল থাকবে এখানে
│   └── app.ts
│
├── dist/                   <-- কম্পাইল হওয়া .js ফাইলগুলো যাবে এখানে
│
└── tsconfig.json           <-- কনফিগারেশন ফাইল
```
---

### 🏃 রান করার প্রক্রিয়া

### ১. `.ts` ফাইল কম্পাইল করো:

```bash
tsc
```

এর ফলে `src/app.ts` → `dist/app.js` এ কম্পাইল হবে।

### ২. তারপর `.js` ফাইল রান করো:

```bash
node dist/app.js
```

---

###  ছোট্ট একটা সম্পূর্ণ উদাহরণ

### 👉 `src/app.ts`:

```javascript
let msg: string = "Hello TypeScript with config!";
console.log(msg);
```

### তারপর কম্পাইল করো:

```bash
tsc
```

### তারপর রান করো:

```bash
node dist/app.js
```

Output:

```
Hello TypeScript with config!
```