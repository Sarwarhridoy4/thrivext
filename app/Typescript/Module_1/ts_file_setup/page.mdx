## 🟢 প্রাথমিকভাবে TypeScript রান করা (Without Setup)

ধরো তুমি শুধু TypeScript শেখার জন্য প্র্যাকটিস করছো — তখন খুব সহজে নিচের ধাপগুলোতে কাজ করা যাবে।

### 👉 ১. একটা `.ts` ফাইল তৈরি করো:

```bash
echo "let msg: string = 'Hello TypeScript'; console.log(msg);" > app.ts
```

### 👉 ২. এই ফাইলটি কম্পাইল করো:

```bash
tsc app.ts
```

### 👉 ৩. তারপর রান করো:

```bash
node app.js
```

**Output:**

```
Hello TypeScript
```

---

## ⚠️ কিন্তু সমস্যা কী?

এভাবে সরাসরি `.ts` → `.js` এ রূপান্তর করে কাজ করলে অনেক অসুবিধা হয়:

| সমস্যা | কারণ |
| --- | --- |
| ❌ `.ts` ও `.js` ফাইল একসাথে মিশে যায় | সব ফাইল একই ফোল্ডারে থাকে |
| ❌ বড় প্রজেক্টে structure রাখা কঠিন | কোন ফাইল কোথায় যাচ্ছে তা বোঝা যায় না |
| ❌ টিমে কাজ করলে কনফিগারেশন মিসম্যাচ হয় | কে কোন ফোল্ডার থেকে রান করবে, কোন ফোল্ডারে কম্পাইল হবে সেটা অস্পষ্ট |
| ❌ source code আর compiled code আলাদা না থাকায় version control-এ সমস্যা হয় | `.js` ফাইলগুলো Git-এ না রাখাই ভালো, কিন্তু এখানে আলাদা করা যাচ্ছে না |

## 🛠️ `tsconfig.json` সেটআপ

যখন তুমি একটু সিরিয়াসভাবে বা টিমে/প্রজেক্টে কাজ করবে — তখন নিচের মতো সেটআপ করাই ভালো: TypeScript প্রজেক্টে কীভাবে `.ts` ফাইল কম্পাইল হবে, তা কনফিগার করার জন্য `tsconfig.json` দরকার হয়। এটি তৈরি করতে:

```bash
tsc --init
```

এই কমান্ড চালালে প্রজেক্টের রুট ফোল্ডারে একটি `tsconfig.json` ফাইল তৈরি হবে।

---

### ⚙️ `tsconfig.json`এ `in` এবং `outDir` সেটআপ

ধরো আমরা চাই আমাদের `.ts` ফাইল থাকবে `src` ফোল্ডারে এবং কম্পাইল হওয়া `.js` ফাইলগুলো থাকবে `dist` ফোল্ডারে। তাহলে `tsconfig.json` ফাইলে নিচের মতো করে সেট করতে পারো:

```json
{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "rootDir": "src",
    "outDir": "dist",
    "strict": true},
  "include": ["src"]
}
```

---

### 📁 ফোল্ডার স্ট্রাকচার (উদাহরণ):

```
your-project/
│
├── src/
│   └── app.ts
│
├── dist/         <-- compiled JS files will be here
│
└── tsconfig.json
```

---

### 🏃 রান করার প্রক্রিয়া

### ১. `.ts` ফাইল কম্পাইল করো:

```bash
tsc
```

এর ফলে `src/app.ts` → `dist/app.js` এ কম্পাইল হবে।

### ২. তারপর `.js` ফাইল রান করো:

```bash
node dist/app.js
```

---

### ✅ ছোট্ট একটা সম্পূর্ণ উদাহরণ

### 👉 `src/app.ts`:

```javascript
let msg: string = "Hello TypeScript with config!";
console.log(msg);
```

### 👉 তারপর কম্পাইল করো:

```bash
tsc
```

### 👉 তারপর রান করো:

```bash
node dist/app.js
```

Output:

```
Hello TypeScript with config!
```