# Constraint using `keyof`

## `keyof` এর ব্যবহার

আমরা একটি `Role` নামের interface বানিয়েছি যেখানে ৩টা role আছে: `frontend`, `backend`, `fullstack`।

তারপর `Employee` নামের interface-এ `role` property-টা এমনভাবে বানিয়েছি যাতে সেটা `Role` interface-এর key গুলার মধ্য থেকে হতে হয়।

অর্থাৎ `Role`  `frontend`, `backend` অথবা `fullstack`হতে হবে।

এতে করে **ভুল role দিলে TypeScript সাথে সাথে ধরতে পারবে**।

### উদাহরণ:

```tsx
interface Role {
  frontend: string;
  backend: string;
  fullstack: string;
}

interface Employee {
  name: string;
  age: number;
  email: string;
  role: keyof Role;
  
  // উপরের এই লাইনে আমরা বলেছি: Role নামের interface-এর যেসব key আছে (frontend,        backend, fullstack) — সেগুলার যেকোনো একটাই এখানে role হিসেবে নেওয়া যাবে।

  // role: 'frontend' | 'backend' | 'fullstack';
  // আর এই লাইনে আমরা manual way-তে সরাসরি তিনটা মান দিয়ে দিচ্ছি, অর্থাৎ role-এ শুধু এই      তিনটাই দেওয়া যাবে।

}

const employee1: Employee = {
  name: 'Hamim',
  age: 18,
  email: 'hamim@gmail.com',
  role: "fullstack" // ✅ Role এর ভিতরের key গুলোর মধ্যেই আছে
};
```

## `Constraint` এর সাথে `keyof` এর ব্যবহার

ধরেন, আমাদের একটা object আছে — ধরেন একটা `student` object।

এখন আমরা চাই, এমন একটা function বানাতে যেটাতে ওই object এবং `key` দিলে ওই `key` এর ভ্যালুটা আমাদেরকে রিটার্ন করবে। এর বাইরে কিছু দিলে `error` দিবে।

এই কাজটাই করা যায় `keyof` দিয়ে।

### উদাহরণ:

```tsx
const student = {
  name: "Hamim",
  id: 101,
  age: 18,
};

const getProperty = <T, K extends keyof T>(obj: T, key: K) => {
  return obj[key];
};

const result = getProperty(student, "name"); // ঠিক আছে
const wrong = getProperty(student, "email"); // ⚠️ Error: 'email' does not exist in type
```

### ব্যাখ্যা:

🔸 এখানে আমরা একটা generic function `getProperty` বানাইছি।

🔸 `K extends keyof T` মানে — `K` type টা হবে `T` type এর ভেতরে থাকা কোনো একটা key এর নাম।

🔸যেমন এখানে আমরা `student`পাঠাচ্ছি, তাই key হিসেবে থাকবে

```
'name' | 'id' | 'age'
```

🔸 এখন আমরা যেই key পাঠাবো, সেটা এই তিনটার মধ্যে থাকলেই valid — না হলে error দিবে।

🔸 তাই যখন আমরা `getProperty(student, "name")` দিচ্ছি, সেটা ঠিক আছে কারণ `name` `student` object এ আছে।

🔸 কিন্তু যদি `"email"` দেই — যেটা object এ নাই, তখন TypeScript সাথে সাথে error দেখাবে।