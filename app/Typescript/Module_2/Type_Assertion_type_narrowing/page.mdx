# টাইপ অ্যাসারশন (Type Assertion)

টাইপ অ্যাসারশন মানে হচ্ছে আমরা TypeScript-কে সরাসরি বলে দিচ্ছি —

**"এই ভ্যারিয়েবলের টাইপ আমি জানি, তুমি নিয়ে চিন্তা করো না!"**

আমরা এটা তখনই ব্যবহার করি যখন আমাদের জানা থাকে যে কোনো ভ্যারিয়েবলের টাইপ কী, কিন্তু TypeScript সেটা ঠিকভাবে বুঝতে পারছে না।

### 🧠 উদাহরণ :

```tsx

let value: any = "Hello TypeScript";
let length: number = (value as string).length;
```

# টাইপ ন্যারোইং (Type Narrowing)

টাইপ ন্যারোইং মানে — TypeScript যখন বুঝে ফেলে কোনো ভ্যারিয়েবলের টাইপ কোনটা হতে পারে এবং সেই অনুযায়ী সে আমাদের হেল্প করে।

আমরা অনেক সময় এমন ফাংশন লিখি যেখানে ইনপুট একাধিক টাইপের হতে পারে (যেমন: string | number)।

আমরা নিচে একটা ফাংশন লিখেছি `minuteToSecond`, যেটা `minute` নামে একটা ইনপুট নেয় — সেটা `string` অথবা `number` হতে পারে।

আমরা টাইপ ন্যারোইং ব্যবহার করে বুঝে নিচ্ছি `minute` আসলে কোন টাইপের, তারপর সেই অনুযায়ী কাজ করছি।

```tsx

const minuteToSecond = (minute: string | number): string | number | undefined => {
    if (typeof minute === 'string') {
        return `${minute} minute = ${Number(minute) * 60} second`;
    }
    if (typeof minute === 'number') {
        return minute * 60;
    }
}

```

এখন আমরা `minuteToSecond` ফাংশনটি কল করি, তখন আমরা জানি — কোন টাইপের ইনপুট দিলে কোন টাইপের আউটপুট পাবো।

তাই আমরা নিশ্চিন্তে টাইপ অ্যাসারশন (type assertion) ব্যবহার করে বলে দিতে পারি —

**"এই রিটার্ন ভ্যালুটা এই টাইপই হবে!"**

```tsx

const result1 = minuteToSecond(10) as number;
const result2 = minuteToSecond('10') as string;

```