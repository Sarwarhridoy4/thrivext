# Mapped Types

**Mapped Types** ব্যবহার করে আমরা একটি টাইপের প্রতিটি প্রপার্টির ওপর কাজ করে নতুন টাইপ বানাতে পারি। এটা অনেকটা JavaScript-এর `map()` এর মতোই — কিন্তু টাইপ লেভেলে।

আমাদের কাছে একটি number এর array আছে। এখন আমরা চাই প্রতিটি সংখ্যা কে string এ রূপান্তর করতে।

### উদাহরণ:

```tsx
const arrayOfNumbers: number[] = [2, 3, 4, 5];

// প্রতিটি সংখ্যাকে string এ রূপান্তর করবো
const arrayOfStrings: string[] = arrayOfNumbers.map((num) => num.toString());

console.log(arrayOfStrings); // Output: ['2', '3', '4', '5']
```

## Static Mapped Type

```tsx
type AreaString = {
  height: string;
  width: string;
};

// height, width key-গুলো রেখে তাদের value গুলাকে string টাইপ দিচ্ছি
type AreaNumber = {
  [key in keyof AreaString]: string;
};
```

### 🧠 ব্যাখ্যা:

- `AreaString` টাইপে আছে দুইটি key: `height` ও `width` যাদের value টাইপ হলো `string`।
- `keyof AreaString` মানে `"height" | "width"` — এটি union টাইপ।
- এরপর `[key in keyof AreaString]` মানে: height আর width এই দুইটা key-এর উপর লুপ চালাও এবং নতুন টাইপ তৈরি করো।
- প্রতিটি key-এর জন্য আমরা string টাইপ দিচ্ছি।

🔹 অর্থাৎ, এখানে আমরা শুধু key-গুলো রেখে তাদের value টাইপ **string** বানিয়ে দিচ্ছি।

---

## Dynamic (Generic) Mapped Type

### উদাহরণ: আমরা একটা object টাইপ কপি করবো

```tsx
type CopyObj<T> = {
  [key in keyof T]: T[key];
};

const person: CopyObj<{ name: string; age: number }> = {
  name: 'Hamim',
  age: 18
};

console.log(person); // 👉 { name: 'Hamim', age: 18 }
```

---

### 🧠 ব্যাখ্যা:

আমরা `CopyObj<T>` নামে একটা **জেনেরিক টাইপ** বানিয়েছি।

এখানে কী ঘটছে ধাপে ধাপে:

1. `T` মানে — আমরা যেকোনো টাইপ পাঠাতে পারি (উদাহরণ: `{ name: string; age: number }`)।
2. `keyof T` মানে — `T` টাইপের সব key (এই ক্ষেত্রে `'name' | 'age'`)।
3. `[key in keyof T]` — মানে আমরা এই দুইটা key এর উপর লুপ চালাচ্ছি।
4. `T[key]` — মানে প্রতিটা key-এর জন্য আমরা সেই key-এর আসল value টাইপটাই রাখছি।
5. 

```
type CopyObj<T> = {
  [key in keyof T]: T[key];
  এখানে,
  T = { name: string; age: number }  আমরা এইটা পাচ্ছি
  keyof T মানে 'name' | 'age'
  [key in keyof T] — মানে  name:T[key]; age: T[key];
  T একটা অবজেক্ট। অবজেক্ট এর পরে [] এর ভিতর key দিলে value পেয়ে যাই

  T[key] = string;
  T[key] = number;
};

```

এভাবে `CopyObj` যেকোনো টাইপকে "এক্স্যাক্টলি কপি" করে নতুন একটা টাইপ বানায়।

---

### ❓কেন এমনভাবে করলাম?

“আমরাতো সরাসরি লিখতে পারি `const person: { name: string; age: number } = ...` — তাহলে `CopyObj` দিয়ে করব কেন?”

👉 এর উত্তর হলো:

**Dynamic বা Reusable টাইপ তৈরি করার জন্য**। ধরেন আমরা এমন ২০টা টাইপ নিয়ে কাজকরছি, সবগুলোকেই dynamic ভাবে রিইউজ করতে চাই— তখন এই Generic Mapped Type আমরাদের একটাই টাইপ বানিয়ে সব জায়গায় ইউজ করতে সাহায্য করবে।

---

### ✅ আরেকটা Example (with optional values):

```tsx
type OptionalObj<T> = {
  [key in keyof T]?: T[key];
};

const user: OptionalObj<{ username: string; email: string }> = {
  username: 'hamim'
  // email না দিলেও চলে, কারণ ? দিয়ে optional বানিয়েছি
};
```

এখানে `OptionalObj<T>` সব property কে optional বানিয়ে ফেলেছে।