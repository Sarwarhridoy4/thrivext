# Function with Generics

আগে আমরা ফাংশনে টাইপ রিটার্ন করার সময় **static ভাবে** বলে দিতাম কোন টাইপ রিটার্ন হবে, যেমন:

```tsx
function add(a: number, b: number): number {
  return a + b;
}
```

🔹 এখানে return টাইপটা `number` হিসেবে আগে থেকেই fix করে দেওয়া হয়েছে।

কিন্তু যদি আমরা চাই একটা **dynamic function** বানাতে, যেটা যেই টাইপের ইনপুট নিবে, সেই টাইপেরই ডেটা রিটার্ন করবে — তখন আমরা **Generics** ব্যবহার করি।

## Generics ফাংশনের সাথে ব্যবহার

```tsx
function identity<T>(value: T): T {
  return value;
}

const output1 = identity<number>(123);      // number
const output2 = identity<string>("Hello");  // string
```

🔹 এখানে `identity<T>` তে  `T` প্যারামিটার আকারে কাজ করতেছে।আমরা যদি `string` পাঠাই তাহলে এটি `value` &  `return` এর জন্য `string`  টাইপ সেট করবে।

🔹 `identity` function যেই টাইপের ইনপুট নেয়, সেই টাইপেরই ডেটা রিটার্ন করে।