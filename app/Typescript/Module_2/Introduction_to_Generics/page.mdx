# TypeScript: Introduction to Generics

## Generics কী?

Generics  এটি এমন একটি পদ্ধতি, যার মাধ্যমে আমরা `type` **নির্দিষ্ট না করেই** বিভিন্ন টাইপ নিয়ে কাজ করতে পারি। এর মাধ্যমে কোডে reusability বাড়ানো হয় এবং টাইপ সেফটি (type safety) বজায় থাকে।

Generics ব্যবহারের মাধ্যমে আমরা টাইপগুলি **dynamic** ভাবে প্রয়োগ করতে পারি, যার ফলে কোডকে আরও simple এবং শক্তিশালী করা যায়।

### **Array এর সাথে Generics ব্যবহার**

```tsx
type ArrayType<T> = T[];

const numberArray: ArrayType<number> = [1, 2, 3];
const stringArray: ArrayType<string> = ["apple", "banana"];
```

🔹 এখানে, আমরা `T[]` দিয়ে Array-এর টাইপ flexible করেছি। যেকোনো ধরনের array pass করা যায় — number, string, বা অন্য কিছু।  `type ArrayType<T>` এখানে `T`,  টাইপ প্যারামিটার হিসেবে ব্যবহার করা হয়েছে।

### **Multiple Types**

```tsx
type Pair<T, U> = [T, U];

const pair1: Pair<number, string> = [1, "apple"];
const pair2: Pair<string, boolean> = ["yes", true];
```

### **Object Type**

এখন, আমরা object-এর টাইপেও generics ব্যবহার করতে পারি, যাতে object এর properties dynamicভাবে পরিবর্তন করা যায়:

```tsx
type Person<T> = {
  name: string;
  value: T;
};

const person1: Person<number> = { name: "John", value: 25 };
const person2: Person<string> = { name: "Jane", value: "Developer" };
```

🔹 এখানে, `Person<T>` টাইপটি ব্যবহার করা হয়েছে, যেখানে `T` হলো সেই property `value` এর টাইপ। এর ফলে `value` এর টাইপ `number` ,`string` বা অন্য যেকোনো টাইপ হতে পারে।