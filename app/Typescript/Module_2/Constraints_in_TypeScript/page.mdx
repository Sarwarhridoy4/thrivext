# Constraints in TypeScript

Generics use করলে অনেক flexibility পাই — কিন্তু কোনো কোনো সময় আমরা চাই **"T অবশ্যই একটা নির্দিষ্ট টাইপের মধ্যেই হবে"**। এইরকম ক্ষেত্রে আমরা `extends` keyword ব্যবহার করে **constraint** apply করি।

মানে, আমরা generic টাইপের উপর **শর্ত (condition)** বসাতে পারি।

ধরেন, আমরা চাইছি যে `student` দের তথ্যের সঙ্গে একটা `course` যুক্ত করে দিতে।

এখন এই কাজটা করতে গেলে অবশ্যই আমাদের জানা লাগবে সেই `student` এর `name` আর **id**`id` 

আর extra value থাকলেও সমস্যা নাই — কিন্তু `name` আর `id` অবশ্যই লাগবে।

এই রকম situation এ আমরা Generics এর সাথে constraint (`extends`) ব্যবহার করতে পারি।

### উদাহরণ:

```tsx
interface Student {
  name: string;
  id: number;
}

const addCourseToStudent = <T extends Student>(data: T) => {
  const courseName = "Next Level Web Development";
  return {
    ...data,
    course: courseName,
  };
};

const updatedStudent = addCourseToStudent({ name: "Hamim", id: 101, age: 18 });

console.log(updatedStudent);
// Output:
// { name: 'Hamim', id: 101, age: 18, course: 'Next Level Web Development' }
```

### ব্যাখ্যা:

🔹 এখানে আমরা `Student` নামে একটা interface বানাইছি যেখানে `name` আর `id` property আছে।

🔹 তারপর একটা generic function `addCourseToStudent` বানানো হয়েছে যেটাতে `<T extends Student>` লেখা মানে হলো – function এ যে ডেটা পাঠাবো, তাতে `name` এবং `id` থাকতেই হবে।

🔹 তারপর সেই ডেটার সাথে আমরা নতুন একটা `course` property যুক্ত করছি।

➡️ এইভাবে আমরা dynamic object পাঠাতে পারি যেটাতে extra property (যেমন `age`) থাকলেও সমস্যা নাই, কিন্তু `name` আর `id` থাকতেই হবে — এটাকে বলে **Constraint**।