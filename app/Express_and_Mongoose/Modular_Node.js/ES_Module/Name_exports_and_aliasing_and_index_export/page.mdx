# Name exports, name aliasing & index export
## 📦 **Name Export / Named Export (নেম এক্সপোর্ট)**

নেম এক্সপোর্ট মানে হচ্ছে আমরা যখন কোনো নির্দিষ্ট ফাংশন, ভেরিয়েবল বা ক্লাসকে আলাদাভাবে এক্সপোর্ট করি।

📘 **উদাহরণ:**

```jsx
// mathUtils.js
export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
```

📥 **ইমপোর্ট:**

```jsx
import { add, sub } from './mathUtils.js';
```

👉 এখানে `add` ও `sub` নামগুলো স্পেসিফিকভাবে এক্সপোর্ট এবং সেই নামেই ইমপোর্ট করতে হয়।

---

## ✏️ **Name Aliasing (নেম এলিয়াসিং)**

নেম এলিয়াসিং মানে হচ্ছে এক্সপোর্ট করা ফাংশনের নাম পরিবর্তন করে অন্য নামে ইমপোর্ট করা।

📘 **উদাহরণ:**

```jsx
import { add as addition, sub as subtract } from './mathUtils.js';
```

👉 এখানে আমরা `add` ফাংশনকে `addition` নামে ব্যবহার করছি। এটা অনেক সময় দরকার পড়ে যখন একই নামের ফাংশন থাকে বা নাম ছোট রাখতে চাই।

---

## 🧩 **Index Export (ইনডেক্স এক্সপোর্ট)**

যখন আমরা অনেকগুলো মডিউল বা ফাইলকে এক জায়গা থেকে এক্সপোর্ট করতে চাই, তখন `index.js` ফাইল ব্যবহার করে ইনডেক্স এক্সপোর্ট করি।

📘 **উদাহরণ:**

```jsx
// add.js
export const add = (a, b) => a + b;

// sub.js
export const sub = (a, b) => a - b;

// index.js
export { add } from './add.js';
export { sub } from './sub.js';
```

📥 **ইমপোর্ট:**

```jsx
import { add, sub } from './index.js';
```

👉 এটা বড় প্রজেক্টে কোড ম্যানেজ করার জন্য খুবই সুবিধাজনক।

---