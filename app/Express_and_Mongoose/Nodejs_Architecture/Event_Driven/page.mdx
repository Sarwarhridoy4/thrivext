# 🧩 **Node.js এর Event-Driven Architecture**

Node.js একটি **Event-Driven, Non-Blocking I/O Model** ব্যবহার করে।

### 🎯 Event-Driven Architecture

**Event-Driven Architecture** মানে হচ্ছে এমন একটি কাঠামো, যেখানে সব কিছুই **ঘটনাকেন্দ্রিকভাবে (event-based)** পরিচালিত হয়।

ধরুন, আপনি আপনার **কাজিনকে দোকানে পাঠালেন চিপস আনতে**।

আপনার কাজিন দোকান থেকে চিপস কিনে ফিরে এসে **দরজায় কড়া নাড়ল বা বেল দিল** —

আপনি সেই **ঘটনাটি শুনলেন (event listen করলেন)** এবং **দরজা খুলে দিলেন (response/handle করলেন)**।

Node.js-এও ঠিক একইভাবে কাজ হয়:

- কোনো একটি **event emit** হয়,
- তারপর সেই **event কে listen** করা হয়,
- এবং শেষে সেই ইভেন্ট অনুযায়ী **callback ফাংশনের মাধ্যমে response** দেওয়া হয়।

এই ইভেন্ট-কেন্দ্রিক কাঠামোর ফলে Node.js হয়ে ওঠে দ্রুত, efficient এবং highly responsive একটি runtime।

![screencapture-web-programming-hero-level2-batch-5-video-level2-batch-5-12-4-high-level-overview-of-node-js-architecture-2025-06-14-09_46_00 (1) (1).png](../images/screencapture-web-programming-hero-level2-batch-5-video-level2-batch-5-12-4-high-level-overview-of-node-js-architecture-2025-06-14-09_46_00_(1)_(1).png)

### 📌 মূল ধারণা:

- Node.js সার্ভার সব কাজকে Event আকারে ধরে।
- প্রতিটি Event এর জন্য একটি Callback Function থাকে।
- কোনো কাজ (যেমন ফাইল পড়া, API কল) শুরু করে Node.js অন্য কাজ করতে থাকে।
- কাজ শেষ হলে Event Trigger হয় → Callback চলেঃ

### 🎯 উদাহরণ:

```jsx
fs.readFile('file.txt', (err, data) => {
  console.log(data.toString());
});
console.log("Reading file...");
```

**Output:**

```
Reading file...
[File content]
```

👉 এখানে `readFile` কাজটি async → ফাইল পড়া চলাকালীন অন্য কাজ (console log) আগে হয়।

---

## 🔄 **Event Loop কীভাবে কাজ করে?**

Node.js এর core-এ **Event Loop** কাজ করে। এটি মূলত JS কোডের Execution এবং Callback গুলো পরিচালনা করে। Event Loop is called the heart of node.js. 

### 📚 Event Loop-এর ধাপসমূহ:

1. **Call Stack**: JS কোড এক এক করে Stack-এ ঢোকে এবং Execute হয়।
2. **Web APIs / Node APIs**: Async কাজগুলো (I/O, Timer, Network) এখানে যায়।
3. **Callback Queue**: Async কাজ শেষে Callback এখানে এসে জমা হয়।
4. **Event Loop**:
    - Stack খালি থাকলে, Callback Queue থেকে ফাংশন এনে Stack-এ ঢোকায়।
    - এভাবে Non-Blocking Execution হয়।

### 📊 Visualization:

![_- visual selection (7).png](../images/_-_visual_selection_(7).png)

---