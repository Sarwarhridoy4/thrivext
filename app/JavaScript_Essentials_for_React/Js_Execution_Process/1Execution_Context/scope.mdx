 ### স্কোপ চেইন
    ---
    স্কোপ চেইন হলো ভেরিএভল একটি ধাপ অনুযায়ী সাজানে তালিকা।  যেখানে বর্তমান স্কোপ থেকে শুরু করে উপরের(প্যারেন্ট) স্কোপগুলো পর্যন্ত খুজে দেখা হয় যে ভ্যারিয়ভল টি কোথায় আছে। কাজ :
    
    চল তাহলে একটা উদাহরণ আর চিত্রের মাধ্যমে বুঝি **Scope Chain** কভাবে কাজ করে 👇
    
    ---
    
    ### 🧠 উদাহরণ:
    
    ```jsx
    
    var a = "global";
    
    function outer() {
      var b = "outer";
    
      function inner() {
        var c = "inner";
        console.log(a); // কোথা থেকে পাবে?
        console.log(b); // outer scope থেকে
        console.log(c); // নিজের ভিতরের scope থেকে
      }
    
      inner();
    }
    
    outer();
    ```
    
    ---
    
    ### 🔗 Scope Chain কেমন কাজ করে?
    
    যখন `inner()` ফাংশন `console.log(a)` লিখে `a` ভ্যারিয়েবল খুঁজে:
    
    1. প্রথমে **নিজের ভিতরের scope** এ দেখে (`c` আছে, কিন্তু `a` নেই)
    2. তারপর **parent scope** অর্থাৎ `outer()` ফাংশনের ভিতর দেখে (`b` আছে, `a` নেই)
    3. তারপর **সবচেয়ে উপরের scope** — অর্থাৎ **global scope** এ গিয়ে `a = "global"` খুঁজে পায়
    
    ---
    
    ### 📊 Scope Chain Diagram
    
    ```sql
    inner() Scope
       ↓
    outer() Scope
       ↓
    Global Scope
    ```
    
    JavaScript এমনভাবে স্কোপ চেইন ধরে ধরে খুঁজে দেখে – একদম উপরে না পাওয়া পর্যন্ত।
    
    ---
    
    এটাই হচ্ছে Scope Chain-এর কাজ। একটা ভ্যারিয়েবল খুঁজতে গেলে JavaScript নিচ থেকে উপরের দিকে খুঁজে চলে যতক্ষণ না সেটা পায়।